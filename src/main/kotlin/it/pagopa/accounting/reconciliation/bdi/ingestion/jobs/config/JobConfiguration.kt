package it.pagopa.accounting.reconciliation.bdi.ingestion.jobs.config

/**
 * Job configuration common parameter class: every job have its own configuration class that will
 * extend
 */
sealed class JobConfiguration
